# Warcraft III Public Test Realm (Internal) 版本追踪配置
# source: api 表示使用 BlizzTrack 官方 API 抓取（更准确稳定）
# source: html 表示使用网页 HTML 正则抓取（旧模式）
#
# API 模式字段：
#   source:   "api"
#   tact:     产品代码（如 w3d）
#   timezone: 目标时区
#
# HTML 模式字段（兼容旧配置）：
#   source:   "html"（或不填）
#   url:      网页地址
#   sections: 各区块的正则提取规则

name: "Warcraft III Public Test Realm (Internal)"
source: "api"
tact: "w3d"
timezone: "Asia/Shanghai"
log_file: "Warcraft III Public Test Realm (Internal)"
log_format: "{time}    {version}"
time_format: "%Y-%m-%d %H:%M"
